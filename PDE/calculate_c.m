function c = calculate_c(X, Y)
    % 计算参数
    v = 1.1;
    r = sqrt((X - 1).^2 + (Y - 0.5).^2); % 计算到圆心的距离
    c = zeros(size(r)); % 初始化c

    % 在以(1, 1/2)为圆心，0.1为半径的圆内设置c值
    c(r <= 0.1) = 1;

    % 圆外区域的c值
    c(r > 0.1) = (v - r(r > 0.1)).^2 / (v - 0.1);
end
